---
css: |
  .frontmatter {
    display:flex;
    flex-wrap:wrap;
    gap: 2rem;
    margin-top: 2rem;
    & > * {
      flex-grow: 0;
      flex-shrink: 1;
      flex-basis: 20em;
      &:first-child {
        flex-grow: 2;
      }
    }
  }
---
<div class="frontmatter">
{{ content | safe }}
</div>